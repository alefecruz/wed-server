import { Router } from 'express'

const guestRoutes = Router()

guestRoutes.get('/', () => {
    // TODO
})

guestRoutes.get('/:id', () => {
    // TODO
})

guestRoutes.post('/', () => {
    // TODO
})

guestRoutes.put('/edit/:id', () => {
    // TODO
})

guestRoutes.delete('/:id', () => {
    // TODO
})

export default guestRoutes
